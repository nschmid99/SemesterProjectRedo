//Natasha Schmid CRCP 4391
//Semester Project
let mov;  //constant for movie
function setup() {
  createCanvas(480, 480);  //canvas size
  mov=createVideo("vid.mov");  //link to video file
}

function draw() {  
  mov.loadPixels();  //load the pixels
  for(let i=0;i<300;i++){  
    const x=random(width);
    const y=random(height);
  }
  
  function manipPix(){
    mov.loadPixels();
    
    for (int y=0; y < mov.height; y++) {
    for (int x=0; x < mov.width; x++) {
      
    }}
  }
}